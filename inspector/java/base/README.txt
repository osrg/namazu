Please run `mvn package` in this directory, then you will get target/earthquake-inspector.jar.
The jar file includes the contents of protobuf and byteman so that you don't have to concern the class path.
